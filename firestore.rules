rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 내부 업무용 — 로그인 없이 index.html에서 직접 접근합니다.
    // Firebase Console → Firestore → 규칙 에서 배포하세요.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
